if [ $# -lt 2 ]; then
    echo "Converts the raw file from the arduino to .ivs, .gnu, and a regressed .gnu"
    echo "Usage: ./convert.sh <raw file name> <output file name>"
else 
    echo "not less than 2 inputs"
fi